# Local Postgres, provisioned via docker-compose.yml at the repo root.
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/ajaia_dev?schema=public"
JWT_SECRET="change-me-in-real-env"
PORT=4000
